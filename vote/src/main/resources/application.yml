server:
  port: 8443
spring:
  application:
    name: blog_system
  datasource:
    url: jdbc:mysql://localhost:3306/vote_system?characterEncoding=utf-8&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: QAZPLM123

    hikari:
      minimum-idle: 3
      maximum-pool-size: 10
      max-lifetime: 30000   #不能小于30秒，否则默认回到1800秒
      connection-test-query: SELECT 1

  jpa:
    show-sql: true
  redis:
    port: 6379
#    host: localhost
#    password:
image.save-path: E:\tmp_workspace\upload